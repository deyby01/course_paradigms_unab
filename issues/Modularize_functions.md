# Issue: Modularize functions

## Description
To improve the structure and modularity of the code, create `.h` files to declare functions and `.cpp` files to implement them. Then import the new files into `main.cpp` to achieve a cleaner code structure.